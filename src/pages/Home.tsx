import { Layout } from '../components/common/Layout';

import { ProductList } from '../components/ProductList';

export const Home = () => {
  return (
    <Layout>
      <ProductList />
    </Layout>
  );
};
