import ProductListPage from './ProductListPage';

export default {
  title: 'Pages/ProductListPage',
  component: ProductListPage,
  argTypes: {},
};

const Template = () => <ProductListPage />;

export const Default = Template.bind({});
