import React from 'react';
import Styled from './OrderPage.styles';

const OrderPage = () => {
  return (
    <Styled.Root>
      <div>주문결제페이지</div>
    </Styled.Root>
  );
};

export default OrderPage;
