import styled from 'styled-components';
import Container from '../../components/shared/Container';

export const ShoppingCartPageInnerContainer = styled(Container)`
  flex-direction: row;
  justify-content: space-between;
  padding: 3rem 1.5rem 0;
`;
