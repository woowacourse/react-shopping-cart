import React from 'react';
import Styled from './OrderListPage.styles';

const OrderListPage = () => {
  return (
    <Styled.Root>
      <div>주문목록 페이지</div>
    </Styled.Root>
  );
};

export default OrderListPage;
