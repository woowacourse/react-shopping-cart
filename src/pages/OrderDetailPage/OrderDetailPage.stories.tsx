import OrderDetailPage from './OrderDetailPage';

export default {
  title: 'Pages/OrderDetailPage',
  component: OrderDetailPage,
  argTypes: {},
};

const Template = () => <OrderDetailPage />;

export const Default = Template.bind({});
