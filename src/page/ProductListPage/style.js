import styled from 'styled-components';

const ProductListPageWrapper = styled.div`
  display: grid;
  gap: 10px;
  grid-template-columns: repeat(4, 1fr);
  margin: 140px 10%;
`;

export {ProductListPageWrapper};
