import styled from 'styled-components';

const Styled = {
  UnderlineText: styled.div`
    width: fit-content;
    height: 23px;
    border-bottom: 8px solid rgba(42, 193, 188, 0.5);
    box-sizing: border-box;
    font-weight: 700;
    font-size: 20px;
    line-height: 27px;
    letter-spacing: 0.5px;
  `,
};

export default Styled;
