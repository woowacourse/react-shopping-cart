import styled from 'styled-components';
import PALETTE from '../../../constants/palette';

export const PriceInfoTitle = styled.div`
  font-size: 1.5rem;
  border-bottom: 3px solid ${PALETTE.GRAY_004};
  padding: 1.4rem;
`;
