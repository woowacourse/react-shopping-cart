import StyledHeader from "./header/Header.styled";
import StyledLogoLink from "./header/logo-link/LogoLink.styled";
import StyledWrapper from "./Wrapper.styled";
import StyledMenu from "./header/menu/Menu.styled";

const S = {
  Header: StyledHeader,
  LogoLink: StyledLogoLink,
  Menu: StyledMenu,
  Wrapper: StyledWrapper,
};

export default S;
