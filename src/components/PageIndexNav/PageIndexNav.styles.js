import styled from '@emotion/styled';

const PageButtonContainer = styled.div`
  width: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  margin-top: 50px;
  font-size: 2em;
`;

const PageIndex = styled.span`
  margin: auto 50px;
`;

export { PageButtonContainer, PageIndex };
