import styled from "@emotion/styled";

export const ItemDetailsSkeleton = styled.div`
  display: flex;
  flex-direction: column;
  gap: 40px;
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
`;
