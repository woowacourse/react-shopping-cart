import Loading from '.';

export default {
  component: Loading,
  title: 'components/Loading',
};

const Template = (args) => <Loading {...args} />;

export const Default = Template.bind({});

Default.args = {};
