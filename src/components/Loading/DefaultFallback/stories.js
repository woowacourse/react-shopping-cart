import DefaultAnimation from '.';

export default {
  component: DefaultAnimation,
  title: 'components/Loading/DefaultAnimation',
};

const Template = (args) => <DefaultAnimation {...args} />;

export const Default = Template.bind({});

Default.args = {};
