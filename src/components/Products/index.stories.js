import Products from ".";

export default {
  title: "Presentational",
  component: Products,
};

// const Template = (args) => <Products {...args} />;

// export const ProductsTemplate = Template.bind({});
// ProductsTemplate.args = {
//   data:,
//   loading:
// };
