import { css } from "@emotion/react";

export const CartResultGuideContainerStyle = css({
  display: "flex",
  alignItems: "center",

  gap: "4px",
});

export const CartResultGuideStyle = css({
  fontSize: "12px",
  fontWeight: "500",
});

export const CartResultsContainerStyle = css({
  display: "flex",
  flexDirection: "column",
  gap: "12px",
});
