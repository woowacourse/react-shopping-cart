import { css } from "@emotion/react";

export const OrderCartItemContainerStyle = css({
  display: "flex",
  flexDirection: "column",
  gap: "12px",
  marginBottom: "12px",
});

export const OrderItemQuantityStyle = css({
  fontSize: "12px",
  fontWeight: "500",
});
