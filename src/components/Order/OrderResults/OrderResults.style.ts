import { css } from "@emotion/react";

export const OrderResultGuideContainerStyle = css({
  display: "flex",
  alignItems: "center",

  gap: "4px",
});

export const OrderResultGuideStyle = css({
  fontSize: "12px",
  fontWeight: "500",
});

export const OrderResultsContainerStyle = css({
  display: "flex",
  flexDirection: "column",
  gap: "12px",
});
