import { css } from '@emotion/react';

const overlayLayout = css`
  background-color: rgba(0, 0, 0, 0.35);
  width: 100%;
  height: 100%;
  justify-content: center;
  align-items: center;
  position: absolute;
  inset: 0;
`;
export { overlayLayout };
