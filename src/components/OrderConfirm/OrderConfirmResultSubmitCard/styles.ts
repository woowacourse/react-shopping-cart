import styled from 'styled-components';
import ResultSubmitCard from '../../shared/ResultSubmitCard';

export const StyledOrderConfirmResultSubmitCard = styled(ResultSubmitCard)`
  margin-top: 3rem;
`;
