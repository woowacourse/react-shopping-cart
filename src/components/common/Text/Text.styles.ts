import { css } from 'styled-components';
import { theme } from '../../../styles';

export const TextStyle = {
  title: css`
    ${theme.font.title}
  `,
  body: css`
    ${theme.font.body}
  `,
  caption: css`
    ${theme.font.caption}
  `,
};
