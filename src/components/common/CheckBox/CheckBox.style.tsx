import styled from "@emotion/styled";

export const Button = styled.button`
  border: none;

  width: 24px;
  height: 24px;
  cursor: pointer;
`;

export const Icon = styled.img`
  width: auto;
  height: auto;
`;
