import { styled } from 'styled-components';

export const Selector = styled.select`
  height: 42px;
  width: 102px;

  font-size: 16px;
  margin-right: 30px;

  padding: 0 8px;

  border-radius: 10px;
`;
