import styled from 'styled-components';

export const HeaderStyle = styled.section`
  display: flex;
  background-color: #000000;
  width: inherit;
  height: 64px;
  position: fixed;
  z-index: 1;

  img {
    margin-left: 24px;
  }

  .header_logo {
    width: 56px;
  }

  .header_arrow {
    width: 3%;
  }
`;
