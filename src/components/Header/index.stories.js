import Header from ".";

export default {
  title: "DongKKo",
  component: Header,
};

const Template = (args) => <Header {...args} />;

export const HeaderTemplate = Template.bind({});
HeaderTemplate.args = {};
