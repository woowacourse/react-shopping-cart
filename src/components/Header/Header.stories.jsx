import Header from './Header';

export default {
  title: 'Header',
  component: Header,
};

function Template() {
  return <Header />;
}

export const Default = Template.bind({});

Default.args = {};
