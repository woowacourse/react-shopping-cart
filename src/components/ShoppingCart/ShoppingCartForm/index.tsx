import styled from 'styled-components';

const ShoppingCartForm = styled.form`
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  padding: 3rem 0 0 1.5rem;
`;

export default ShoppingCartForm;
