import styled from '@emotion/styled';

const Root = styled.div`
  width: 100%;
  max-width: 1320px;
  margin: 0 auto;
  color: ${(props) => props.theme.color.defaultBlack};
`;

export default { Root };
