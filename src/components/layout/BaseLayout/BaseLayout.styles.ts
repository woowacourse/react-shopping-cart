import styled from '@emotion/styled';

const Root = styled.div`
  width: 100%;
  max-width: 1320px;
  min-width: 768px;
  margin: 0 auto;
  color: ${(props) => props.theme.color.defaultBlack};
`;

export default { Root };
