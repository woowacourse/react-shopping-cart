import styled from "styled-components";

export const StyledCheckboxButton = styled.button`
  background-color: transparent;
  border: none;
  padding: 0;
  margin: 0;

  cursor: pointer;

  &:focus {
    outline: none;
  }
`;

export const StyledCheckboxButtonImg = styled.img`
  width: 24px;
  height: 24px;
`;
