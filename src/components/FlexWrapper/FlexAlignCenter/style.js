import styled from 'styled-components';

import FlexWrapperStyled from '../style';

const FlexAlignCenterStyled = styled(FlexWrapperStyled)`
  align-items: center;
`;

export default FlexAlignCenterStyled;
