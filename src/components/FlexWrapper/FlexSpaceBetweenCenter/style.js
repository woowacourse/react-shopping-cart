import styled from 'styled-components';

import FlexWrapperStyled from '../style';

const FlexSpaceBetweenCenter = styled(FlexWrapperStyled)`
  align-items: center;
  justify-content: space-between;
`;

export default FlexSpaceBetweenCenter;
