import { css } from "@emotion/react";

const infoLayout = css`
  display: flex;
  align-items: center;
  gap: 4px;
`;

const infoImg = css`
  width: 16px;
  height: 16px;
`;

const intoText = css`
  font-weight: 500;
  font-size: 12px;
`;

export { infoLayout, infoImg, intoText };
