import { css } from "@emotion/react";

export const strongTextStyle = css({
  fontSize: "16px",
  fontWeight: "700",
});

export const couponContainerStyle = css({
  display: "flex",
  flexDirection: "column",
  gap: "12px",
});

export const couponDescriptionStyle = css({
  display: "flex",
  flexDirection: "column",
  gap: "2px",

  fontSize: "12px",
  fontWeight: "500",
});
