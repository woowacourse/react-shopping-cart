import { css } from '@emotion/react';
import { theme } from '../../../styles';

export const TextStyle = {
  title: css`
    ${theme.font.title}
  `,
  body: css`
    ${theme.font.body}
  `,
  caption: css`
    ${theme.font.caption}
  `,
};
