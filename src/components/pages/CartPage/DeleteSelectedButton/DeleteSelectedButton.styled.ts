import styled from 'styled-components';
import Colors from '../../../../constants/Colors';

export const DeleteSelectedButton = styled.button`
  width: 98px;
  height: 35px;

  border: 1px solid ${Colors.DELETE_SELECTED_BUTTON};

  font-weight: 400;
  font-size: 16px;
  line-height: 21px;
  text-align: center;
`;
