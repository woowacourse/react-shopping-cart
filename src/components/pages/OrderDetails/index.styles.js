import styled from 'styled-components';

export const OrderDetailWrapper = styled.div`
  border: 1px solid var(--color-grey-200);
  max-width: 50rem;
  margin: 1rem auto;
`;

export const PaymentSheetWrapper = styled.div`
  justify-content: flex-end;
  margin-top: 1rem;
  display: flex;
  width: 100%;
  max-width: 50rem;
  margin: 1rem auto;
`;
