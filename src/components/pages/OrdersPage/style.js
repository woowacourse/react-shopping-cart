import styled from 'styled-components';
import PALETTE from '../../../constants/palette';

export const OrdersPageContainer = styled.div`
  height: 100%;
  min-height: calc(100vh - 80px);
  background-color: ${PALETTE.GRAY_006};
`;
