import { css } from "@emotion/react";

export const CartTitleStyle = css({
  fontSize: "24px",
  fontWeight: "700",
  textAlign: "left",

  marginBottom: "12px",
});

export const CartItemTypesCountStyle = css({
  fontSize: "12px",
  fontWeight: "500",
  textAlign: "left",
});
