import { ROUTE } from './route';
import { SCHEMA } from './schema';
import { CONFIRM_MESSAGE } from './message';

const AMOUNT_COUNT = {
  MIN: 1,
  MAX: 20,
};

const AMOUNT_COUNTER_FLAG = {
  UP: 'up',
  DOWN: 'down',
};

const IMAGE_SIZE = {
  SM: 'SM',
  MD: 'MD',
};

export { ROUTE, AMOUNT_COUNT, SCHEMA, CONFIRM_MESSAGE, AMOUNT_COUNTER_FLAG, IMAGE_SIZE };
