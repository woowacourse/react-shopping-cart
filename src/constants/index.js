import { ROUTE } from './route';
import { SCHEMA } from './schema';
import { CONFIRM_MESSAGE } from './message';

const AMOUNT_COUNT = {
  MIN: 1,
  MAX: 20,
};

const AMOUNT_COUNTER_FLAG = {
  UP: 'up',
  DOWN: 'down',
};

const CONTENT_PER_PAGE = 11;

const CUSTOMER_ID = 0;

export { ROUTE, AMOUNT_COUNT, CONTENT_PER_PAGE, SCHEMA, CONFIRM_MESSAGE, AMOUNT_COUNTER_FLAG, CUSTOMER_ID };
