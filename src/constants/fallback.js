import noImage from '../assets/image/noImage.jpeg';

const FALLBACK = {
  PRODUCT: {
    IMG_URL: noImage,
    NAME: '상품명 없음',
    PRICE: -1,
    QUANTITY: 1,
    CHECKED: true,
  },
};

export default FALLBACK;
