import React from 'react';

import Cart from 'templates/Cart';

export default {
  title: 'Template/Cart',
  component: Cart,
};

const Template = () => <Cart />;

export const Primary = Template.bind({});
