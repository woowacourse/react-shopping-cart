const color = {
  black: "#000000",
  white: "#ffffff",
};

const fontWeight = {
  light: 300,
  normal: 400,
  medium: 500,
  bold: 700,
  ExtraBold: 800,
};

const fontSize = {
  xs: "0.8rem",
  sm: "1.2rem",
  md: "1.6rem",
  lg: "2rem",
  xl: "2.4rem",
};

const theme = {
  fontSize,
  color,
  fontWeight,
};

export default theme;
