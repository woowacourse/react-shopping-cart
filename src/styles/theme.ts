const colors = {
  white: '#ffffff',
  mint: '#2ac1bc',
  lightGray: '#dddddd',
  gray: '#9ca3af',
  black: '#333333',
};

const theme = { colors };

export type Colors = typeof colors;

export default theme;
